# Copy to .env and fill in. The voice MVP uses its OWN database, separate from
# the existing Next.js web app — don't point it at joma_khoroch.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/joma_khoroch_voice
PORT=4000
